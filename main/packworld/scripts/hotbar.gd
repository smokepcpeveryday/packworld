extends Control

#var slot1 = get_node($"1")
@onready var inv = $inv
var slot0 = get_child(0)
var slot1 = get_child(1)
var slot2 = get_child(2)
var slot3 = get_child(3)
var slot4 = get_child(4)
# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#for i in (inv.invsize):
		#var 
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
	
	
	
